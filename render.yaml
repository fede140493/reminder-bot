services:
  - type: web
    name: reminder-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      BOT_TOKEN:
        fromSecret: BOT_TOKEN
